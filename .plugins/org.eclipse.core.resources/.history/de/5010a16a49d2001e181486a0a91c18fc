package api.endPoints;

import api.Payload.User;
import api.Payload.UserDDT;
import io.restassured.http.ContentType;
import io.restassured.response.Response;
import static io.restassured.RestAssured.given;

public class UserEndPoints {


	// CRUD Create, Read(Get), Update, Delete methods
	public static Response getUser() {
		Response response = given().get(Routes.get_URL);
		return response;
	}

	public static Response createUser(UserDDT userLoad) {
		Response response = given()
				.contentType(ContentType.JSON).body(userLoad).when().post(Routes.post_URL);
		return response;
	}

	public static Response updateUser(User payload, int employeeID) {
		Response response = given().contentType(ContentType.JSON)
				.pathParam("EmployeeID", employeeID).body(payload).when().put(Routes.put_URL);
		return response;
	}

	public static Response deleteUser(int employeeID) {
		Response response = given().pathParam("userName", employeeID)
				.delete(Routes.delete_URL);
		return response;
	}
}
